export default {
  title: "How do you cache FlatList.renderItem? Is the below FlatList.renderItem properly cached?",
  tags: ["react-native"],
  spoiler: "Let's take a look at caching FlatList's renderItem when it needs to pass down an ID to the on press of the list's item.",
  getContent: () => import("./document.mdx"),
};
